{
	"crons": [
		{
			"path": "/api/process-feed",
			"schedule": "45 16 * * *"
		}
	],
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/next"
		}
	],
	"buildCommand": "prisma generate && next build",
	"installCommand": "npm install"
}
